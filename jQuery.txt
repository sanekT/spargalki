1. $('.goods')  - получением дом элемента по селектору


2. $('document').ready(function(){
	
});//получаем document и начинаем выполнять js код только после загрузки html страницы  УСТАРЕВШИЙ ВАРИАНТ
2.а. $(function(){

});//получаем document и начинаем выполнять js код только после загрузки html страницы  БОЛЕЕ НОВЫЙ ВАРИАНТ


3. $('.goods').html(html-код)   - получаем элемент и пишем в него html код или получаем html код из этого элемента


4. $('.goods').attr('название атрибута')   -  получаем элемент и потом его значение атрибута по названию


5. $.getJSON('путь к файлу .json', function(data){//получаем с помощью ajax запроса файл json с сервера
	//в data содержится уже распарсенный объект из файла json
});


6. $.isEmptyObject(obj);//метод проверяет пуст ли переданный объект


7. $('.item').on('click', function(){});//вешаем событие click  и функцию для выполнения по клику УСТАРЕВШИЙ ВАРИАНТ
7.а. $('.item').click(function(){});//то же самое но более НОВЫЙ ВАРИАНТ


8. $('.item').next();//находим элемент по селектору и потом находим соседний элемент


9. $('.item').slideToggle(500);//находим элемент и скрываем его плавно, при  повторном нажатии появляется плавно

10. $('.item').fadeToggle(500);//то же самое, ноотображение немного другое